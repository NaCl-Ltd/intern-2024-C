<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<%= form_with url: users_path, method: :get do |f| %>
  <%= f.text_field :search_query %>
  <%= f.submit 'Search' %>
<% end %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>
